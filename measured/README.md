# measured (copy)

These are copies. The canonical source is the `measured/` directory of the
training repo - https://github.com/njtierney/training - where the demo site
and the full documentation live.

They are copied rather than referenced because this book is its own
repository, so a relative path out to a sibling repo would break for anyone
cloning it on its own.

To update: copy the four `.scss` files across again. Eventually this should
become a Quarto extension, at which point `quarto add` replaces the copy.
